# Zaland Collection Local Environment Configuration
# No environment variables are currently required for this pure-frontend static storefront.
# The catalog runs off local JSON files in development and falls back to localStorage in production.
PORT=3000
HOST=0.0.0.0
